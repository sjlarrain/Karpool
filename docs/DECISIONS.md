# Open Decisions Register

Seeded from `00_DEV_ENVIRONMENT_SETUP.md` §9 — this should have shipped at the end of session 1 but
did not. Backfilled at the start of session 2 before any Phase 0 work.

Each decision needs a one-line answer **from the developer**. A recommendation is not a decision —
work only proceeds on a recommendation where explicitly marked "applied" below, and only when the
alternative is effectively irreversible-neutral (a formatting/tooling choice already reflected in
committed code, not a product or data-model judgment call).

| ID | Decision | Recommendation | Status |
|---|---|---|---|
| D-01 | App framework on Vercel | Next.js 15 App Router + TypeScript | **Applied** — `package.json` deps confirm Next 15 + TS; scaffold completed session 2 |
| D-02 | Styling approach | Hand-written CSS + tokens ported from the sketch; no Tailwind | **Applied** — `package.json` has no Tailwind dependency (session 1); `src/styles/tokens.css` ported verbatim session 2 |
| D-03 | Google Maps topology (client-never-talks-to-Maps vs. Maps JS API — the infra plan contradicts itself) | Server-proxied Directions API + lightweight client renderer | Open (topology unanswered) — **Phase 6 is deferred indefinitely, not merely to the end of the build.** Originally moved after Phases 7–9 (developer, 2026-08-16); on **2026-08-19** the developer set a business precondition: build Maps only once the app shows *real traction*, because Maps is a paid API and they will not pay before there is usage. Do not start Phase 6, and do not re-propose it, until the developer says traction is there. |
| D-04 | Supabase auth topology (same client/server contradiction) | `@supabase/ssr` cookie sessions; all data access via Vercel API routes, anon key never used for browser reads | **Decided (2026-08-15)** — recommendation confirmed by developer: server-side only |
| D-05 | PWA service worker | Hand-written service worker + `manifest.webmanifest` | **Applied (2026-08-16, agent judgment call)** — the recorded recommendation was already the only reasonable engineering direction; built exactly as recommended (`public/sw.js`, `public/manifest.webmanifest`). Flagging per usual practice in case the developer wants to revisit. |
| D-06 | Package manager | pnpm | **Applied** — pnpm lockfile in use |
| D-07 | Admin scope — sketch shows group-level admin only; requirement 9 asks for platform-wide admin | Both, as separate roles (`platform_admin`, `group_admin`) | **Applied** — already embodied in `0001_init.sql` (`profile.platform_role`, `membership.group_role`, both `text check` enums with exactly this split); confirmed with developer 2026-08-17 while unblocking Phase 8 |
| D-08 | Cost split — static group setting or computed from distance? | Static per-group field in v1 | **Applied** — `group.cost_split_note` is a static per-group text field; `POST/PATCH /api/groups` already implement it this way |
| D-09 | Guest riders — shadow record or name-only string? | Name-only string on `trip_rider`, no account | **Applied** — `trip_rider.guest_name` is name-only, no profile; close flow inserts guest rows and credits their pooled points to the driver, verified end-to-end |
| D-10 | Late-cancellation penalty — fixed or per-group configurable? | Group-configurable, default −5 pts / 60 min | **Applied** — `group.late_window_minutes`/`late_penalty` drive `POST /api/trips/:id/leave`'s penalty, verified end-to-end |
| D-11 | Leaderboard weights (10/3/2) — global or per-group? | Per-group, defaults 10/3/2 | **Applied** — already implemented (`group.drive_weight`/`pool_weight`/`kudos_weight`, used since the Phase 4 close flow); the leaderboard now reads the same columns |
| D-12 | Leaderboard period — calendar month, rolling 30 days, or toggle? | Calendar month; ledger stays all-time | **Applied (2026-08-16, agent judgment call)** — `GET /api/groups/:id/leaderboard` filters to the current calendar month; `points_ledger` itself is never date-filtered/reset, matching "ledger stays all-time" |
| D-13 | Email domain restriction on group joining? | Not in v1 — group code only | **Decided (2026-08-15)** — recommendation confirmed by developer: group code only |
| D-14 | PII / audit-log data retention — in tension with the plan's own "audit_log is append-only, no delete path anywhere" non-negotiable | Keep every `audit_log` row indefinitely, no redaction/purge job | **Decided (2026-08-17)** — developer: not a priority for a small internal tool; the admin capability itself (fixing stuck trips, ledger mistakes) matters more than a retention policy right now. Revisit only if a real deletion/right-to-erasure need shows up later. |
| D-15 | (referenced by Phase 1 table, no entry recorded in the source doc) | — | **Dropped (2026-08-15)** — developer confirmed this is a documentation error in `02_IMPLEMENTATION_PLAN.md`, not a real decision; no longer blocks Phase 1 |
| D-16 | Trip `scheduled→started` window — "not before T−2h" per the implementation plan, unconfirmed | — | **Decided (2026-08-16)** — developer confirmed: fixed T−2h window |
| D-19 | Scoring rework — driving should scale with kudos and with how many people were pooled on a trip, and missing a pool should cost points | — | **Decided (2026-08-19)** — developer chose, from three costed options each: **(1)** pooling escalates per seat (`pool_weight + (n-1)·pool_step`, defaults 3/5/7 rather than flat 3); **(2)** a kudos is worth `kudos_weight × confirmed riders`, so rating rewards pooling not just driving; **(3)** a no-show costs the rider **−10**, deliberately worse than the −5 late cancellation, because a no-show wastes the seat entirely. All four numbers are per-group columns (migration `0007`). **Forward-only** — `points_ledger` is append-only, so existing scores are untouched and no history is recomputed. Supersedes the `10·driven + 3·pooled + 2·kudos` line in `CLAUDE.md` §4, which the developer needs to update by hand (that file is immutable to the agent). |
| D-18 | Kudos prompt has no "no thanks" path — the sketch's rate overlay has a 💚 toggle and submits either way; the built version only offers "Send kudos", so a rider who doesn't want to give kudos can never clear the prompt off a closed trip | Record the decline (a `kudos.declined` flag or a dismissed-at column) rather than hiding it client-side, so the prompt stays cleared across devices — but this is a product call, not an engineering one | **Decided (2026-08-19)** — developer: "that way after he has cleared the trip they can discard if they don't want to give kudos." Built as recommended: `trip_rider.kudos_declined_at` (migration `0006`), `POST /api/trips/:id/kudos/decline`, and the sketch's 💚 toggle whose submit reads "Send kudos" or "Skip & close". Applied and verified end-to-end. |
| D-17 | Sketch shows a `comment` notification type with no comment UI | Logged as a gap, not built as a feature, per `02_IMPLEMENTATION_PLAN.md` §7 | Open — no UI to build until answered |
| D-20 | Ride share link — what a shared trip URL exposes, and whether it grants access. Trips have no URL today (trip detail is client state), and a WhatsApp-shared link leaves the group by design | Teaser preview for non-members (route, day, time, seats left, driver first name — no pickup addresses, no rider names); actually joining the ride still requires sign-in **and** group membership via the existing `/j/CODE` code flow, so the 6-char code stays the access gate | **Decided (2026-08-22)** — developer: "they will receive the link but they must be logged to get the information." Built stricter than the recommendation, and deliberately so: there is no teaser at all. `/t/:id` reads the trip through the session client, so RLS decides — signed out sees the auth form, a signed-in non-member sees "this ride isn't available to you", a member is redirected into the trip detail overlay. Group membership by code stays the only door. Covered live by `tests/e2e/share-link.spec.ts` |
| D-21 | Scheduler replacement — Vercel Cron was removed (free tier won't run a 5-minute schedule), so `/api/cron/tick` has no caller: **T-15min departure reminders and the 6h auto-close of abandoned `started` trips no longer run** | Supabase `pg_cron` + `pg_net` calling `/api/cron/tick` with the `CRON_SECRET` header — inside the existing service list, free tier, no new vendor | **Decided (2026-08-24)** — developer chose pg_cron over an external pinger. Built in migration `0008`: the `carpool-tick` job runs `*/5 * * * *` and posts through `pg_net`. Neither the URL nor the secret is in the repo — both live in Supabase Vault (`carpool_tick_url`, `carpool_cron_secret`) and are read at call time, and the job no-ops until the developer creates them. Migration `0009` adds `carpool_cron_status()` behind `GET /api/admin/health`, so a dead scheduler is now a visible fact rather than an absence of auto-closes — which is how this went unnoticed for weeks. Known caveat, accepted: a Free-plan Supabase project pauses after 7 days of inactivity and runs no jobs while paused. |
| D-22 | Email confirmation at signup — the built-in Supabase mailer cannot reach anyone outside the project team (2 sends/hour, team-only), so requiring a confirmed email made public registration impossible | Turn **Confirm email** off now and configure custom SMTP before the first feature that genuinely needs email | **Decided (2026-08-26)** — developer disabled email confirmation on the Supabase project rather than wait for SMTP, to get colleagues into the app. Signup now returns a session immediately and lands the visitor in the group. **Accepted trade-offs:** addresses are unverified (the 6-char group code stays the real access gate, per D-13), and a mistyped address becomes silent and unrecoverable because **the app has no password-reset flow at all** — none exists in the codebase. Reversible from the dashboard. The first thing that will need custom SMTP is password reset; `README.md` deploy step 5 has the provider guidance (Brevo — single-sender verification, no domain needed). |

| D-23 | Unstarted trips never expire — `loadGroupTrips` filters on status only, so a `scheduled` trip nobody started stays in the feed forever and stays joinable long past its departure time | A grace window, then a terminal state | **Decided (2026-08-26)** — developer: "they will be able to start late or add other passengers. But after 24 hours it will classify as Past." Built as: self-serve joining stops at `depart_at`; the driver can still start/close the trip, and still add passengers, for 24h after it; still unstarted at `depart_at + 24h` and the scheduler ends it (`status = 'cancelled'`, `cancelled_reason = 'not_started'`, no points, no penalties, riders notified). Shown as **Past · not started**, never as "cancelled", so an expiry doesn't read as the driver calling it off. **Knock-on fix:** the create form only ever published "today at HH:MM", so once joining stopped at `depart_at` an evening publish produced a ride nobody could take — the day is now a field on the form (defaults to today, can't go backwards). The API and the feed already handled multi-day trips; only the form was stuck. |
| D-24 | Driver-added passengers — a driver has no way to seat someone who asked for the ride in person; guests could only be named at close time, where they also bypassed the capacity check | Name-only guests, occupying a seat | **Decided (2026-08-26)** — developer overrode the recommendation: **group members only, picked from a list**, not free-text guests. A driver seats a member on their own trip; the seat counts against capacity, the member is notified, and they may leave without the −5 late-cancellation charge because they never booked it themselves (developer chose "no penalty"). The close-time guest-name field is untouched — it stays the way a non-member is recorded after the fact. |
| D-25 | Feedback capture — no route existed for a user to tell the developer anything | Feedback table + a tab in the existing admin console | **Decided (2026-08-26)** — developer confirmed the recommendation. Email was the alternative and is blocked anyway: the project still has no custom SMTP ([D-22]). Feedback is stored in Postgres and read from the admin console, so nothing depends on mail delivery. |
| D-26 | App name — the code says "Carpool" everywhere while the deployed project is `karpool-nu.vercel.app` | Rename user-facing text and docs; leave internal identifiers | **Decided (2026-08-26)** — developer confirmed: **Karpool** across every visible string, page title, the web manifest, the service worker and the docs. `package.json`'s `name`, the repo folder, the sketch's filename and internal keys (e.g. the install-prompt's `localStorage` key) stay as they are — renaming those churns the diff without changing anything a user sees, and changing the storage key would silently re-show the install prompt to everyone. |
| D-27 | Past trips — a finished trip vanished from the feed 24h after closing, and there is no history screen anywhere, which also made the kudos prompt unreachable once the card dropped out | — | **Decided (2026-08-26)** — developer: "Every finished trip will be under past trips." The Carpools tab grows a **Past** section holding closed, cancelled and expired trips beneath the live ones. Applied default, flagged here rather than assumed: the section loads the **last 30 days** — all-time history is a bigger query and a bigger screen than was asked for. Say the word and it becomes all-time or a different window. |
| D-28 | Product analytics — the app ships no usage measurement at all, so there is no way to tell whether anyone publishes trips, where signup drops off, or whether a group has gone dead. The developer raised **PostHog** specifically (2026-08-26) | Adopt PostHog, or decide explicitly not to. This is a **new third-party service**, which `00_DEV_ENVIRONMENT_SETUP.md` §"Do not add" forbids by default ("an analytics SDK") and `02_IMPLEMENTATION_PLAN.md` §7 lists as out of scope — so it cannot be added without this entry being answered, and answering it supersedes both lines | **Open (raised 2026-08-26)** — feasibility checked, nothing built, no packages installed. **Fit:** clean. Next 15 App Router + Vercel + PWA is a supported combination; `next.config.mjs` carries no CSP to amend and `public/sw.js` has no `fetch` handler to fight; free tier (~1M events/month) is far above an internal tool's volume. **Shape if adopted:** `posthog-js` + `posthog-node`; `NEXT_PUBLIC_POSTHOG_KEY`/`NEXT_PUBLIC_POSTHOG_HOST` added to `.env.example` and to **both** env schemas (`src/env.ts`, `src/env.client.ts`) as `.optional()` so the app still boots unconfigured, exactly like the Maps browser key; a `/ingest` rewrite in `next.config.mjs` to keep ingestion first-party (matters here — it is a phone-installed PWA facing ad blockers and iOS tracking prevention); a client provider mounted in the **root** layout so the signup funnel is covered, not just `/app`; `posthog.identify()` + `posthog.group()` fed from the ids `src/app/app/page.tsx` already holds. **Three sub-questions the developer must answer, none of them engineering calls:** (a) **region** — EU vs US Cloud is permanent per project, and this app holds colleagues' home pickup locations and commute patterns; (b) **session replay** — it would record real coworkers' screens including the roster and pickup addresses, which is a consent conversation rather than a config flag; (c) **scope** — client-only capture undercounts, because a PWA on a commute loses events, so the trip lifecycle (publish/join/leave/start/close/kudos/no-show) wants server-side `posthog-node` capture in the API routes, on the same Supabase user id. **Known friction to resolve if it proceeds:** analytics must never fail a trip join, which means a deliberately-dropped flush error — in tension with `CLAUDE.md` §3.5 "no swallowed errors", so it needs an explicit sanctioned exception rather than a bare `catch {}`. Nothing would go into `src/domain/`; those stay pure. Kept distinct from `src/lib/audit.ts`, which is admin-action-only and stays that way. |
| D-29 | Highlighting a trip that detours — the MBA group exercises before or after university, and a ride that passes the gym is both a scheduling fact riders need and a motivation the app could surface. Nothing in the schema could express "this ride stops somewhere" | Reuse the existing admin-managed `pickup_place` list with a `kind`, one stop per leg on `trip`, and a drawn sign inside the card's route line | **Applied (2026-08-27)** — migration `0012` is live on the project; verified against it with 12 schema checks (constraints, defaults, feed select, on-delete behaviour), all passing, test rows removed. Developer chose, across seven exchanges: **(1)** the place list stays **manager-managed and fixed** ("tags can be overpopulated"), so stops reuse `pickup_place` — whose POST route was already `group_admin`-only — with `kind` separating the two lists; **(2)** **no free-text note** ("it has to be intuitive to recognize a gym trip"), so the sign is a drawn icon from a fixed 8-value vocabulary, not typed text and not an emoji (an emoji renders differently on each phone, and this is an installed PWA); **(3)** a stop is a real **detour**, and it can fall on **either leg** — hence `trip.out_stop_id` / `trip.back_stop_id`, one per leg, each guarded by a CHECK against `direction` in the same idiom as the existing `return_at` check; **(4)** the **whole car stops** — the driver goes too. A per-rider drop-off (the gym as one rider's destination) was the rejected alternative and stays out: it would need a column on `trip_rider` and a "getting off at" control in the join flow. Also out of scope, deliberately: a trip that *ends* at the gym, because the group owns the route and its endpoints don't change; and any dwell-time field, because an hour at the gym and a two-minute drop-off cannot share one number and the app shows no arrival time to adjust. **Presentation, revised (2026-08-27):** the sign was first built *inside* the route line (`Riverside → [Gym] → HQ`). The developer rejected that on sight — "rather to put the stop in the middle, I need you to make a *warning* mention that they will be going to the gym" — so the stop moved out of the route line, which is back to plain `from → to`. Built first as a bordered amber block with a warning triangle; the developer then corrected the framing twice over — "it looks too big proportional to the card" and "it is not a warning per sé, it is just to communicate that they will do something before arriving". Final form: a small tinted chip with the stop's icon and name, followed by muted timing text, no box, border or alert glyph — **right-aligned on the route row**, opposite the origin → destination it qualifies, so the card keeps one horizontal band per fact rather than growing a row per stop. **Final form of the tag** after three more rounds: the place name on top, and the leg as a *centred arrow underneath it* rather than words — straight on for the outbound stop, a sideways U-turn for the return one. The wording (`in way` / `back`) survives as screen-reader-only text and as the tag's `title`, since an arrow alone tells assistive tech nothing; the trip detail overlay still spells the leg out. Note the U-turn is turned so its head finishes pointing **left**: the developer asked for -90°, but that rotation lands the head pointing right, the direction the outbound arrow already owns. This also removed the round-trip second route line, which only existed to show *where* on the route a return stop fell; the mention says so in words instead. **Motivation without scoring:** `stopsThisMonth` on the You tab counts stops the caller drove or rode this month. It writes nothing to `points_ledger` — so it cannot inflate scores against D-19, there is no incentive to lie for points, and dropping the feature later costs one column. |

| D-30 | **One-way riders on a round trip (developer: soon, mechanics given 2026-08-28).** A round trip carries one `capacity` and `trip_rider` has no leg, so a rider who only travels out still holds the return seat, and a colleague who only needs a lift home cannot take one | **Per-leg booking with outbound priority**, exactly as the developer described it — see the flow below | **Open (raised 2026-08-28)** — **The developer's flow, verbatim in intent:** when someone joins a trip that has open seats, they are **asked at join time whether they are returning on it too**. Outbound riders hold **priority** on the return seats: a return-only booking is refused **until the outbound leg is closed**, and only the seats still free at that moment open to everyone else. So return capacity is *reserved* for outbound riders up to that cutoff rather than sold first-come. **Schema — superseded 2026-08-29 by [D-35]'s two-row model; do not build this:** `trip_rider.legs text check (legs in ('out','back','both')) default 'both'`. Under two rows each leg carries its own `capacity` and its own seat count, so `legs` collapses to a boolean "returning?" on the outbound row whose only job is deciding who gets copied — and the `decorateTrip` problem noted below (two seat numbers on one card without doubling its height) disappears outright. **Touches:** `join_trip()` (migration `0010`) counts all active riders inside `select ... for update` — both the capacity check and its locking contract change, and the reservation rule adds a second, time-dependent condition; `POST /api/trips/:id/join` gains a leg argument and the join sheet gains the returning question; `decorateTrip`'s `seatsLeft`/`seatStr` become per-leg and the card must show two numbers without doubling in height. **Questions:** (a) what exactly closes the outbound and releases the held seats — `status='started'`, passing `depart_at`, or an explicit driver action? **D-35 was answered on 2026-08-29 and this now largely falls out of it**: the back trip is materialised when the outbound closes, so a return-only booking is impossible before that moment by construction, and outbound riders hold priority because they are seated on the new row at creation. What survives from this entry is the *declaration* — the join-time "are you returning too?" question — and questions (b)-(d) below; (b) may an outbound rider who declined the return change their mind before the cutoff? (c) does a one-leg rider count as a full seat in D-19's escalating pool weight, or half? (d) a rider dropping only the return leg — full −5, or prorated? |
| D-31 | **Snitch (developer: MID, 2026-08-28)** — a way to accuse someone of having travelled without telling the other riders. Explicitly "just for fun" | Build it **non-scoring** first: a counter and a light badge, not a `points_ledger` entry | **Open (raised 2026-08-28)** — **Distinct from the existing no-show** (−10, D-19): a no-show booked a seat and didn't turn up; a snitch target never booked at all and travelled separately. Both can be true of one person on one day, so the two must not be conflated in the UI. **Why non-scoring is the recommendation:** `points_ledger` is append-only, so a mistaken accusation can never be removed, only offset by a compensating entry — and this is real colleagues accusing each other by name. **Four questions, all social design rather than engineering:** (a) who sees an accusation — the accused, the whole group, or only an aggregate count? (b) is it anonymous? (anonymous + public + named target is the combination to design against) (c) can the accused dispute or clear it? (d) one accusation per person per trip, or unlimited? |
| D-32 | **Per-trip feedback form (developer: MID, 2026-08-28)** | Settle the **audience** before anything is built — it decides which of two existing systems this extends | **Open (raised 2026-08-28)** — It overlaps two things that already exist. **Kudos** (D-18) is already per-rider, per-trip, binary, with an optional comment and a decline path. **D-25 feedback** already stores free text for the developer and is read from the admin console. So: if the audience is the **driver**, this is kudos with more fields and should extend `kudos` rather than sit beside it; if the audience is the **developer**, it is D-25's table plus a `trip_id`; if the audience is the **group admin**, it is neither and needs its own read surface. Building it before answering that produces a third feedback mechanism nobody reads. |
| D-33 | **Group feed (developer: MID, 2026-08-28)** — photos, comments on trips, general group chatter | Comments first, photos second — they carry very different costs | **Open (raised 2026-08-28)** — This would be the app's **first user-generated content** and the first use of **file storage** (Supabase Storage; in the sanctioned service list, unused so far). It also finally answers **D-17**, the sketch's `comment` notification type that has never had a UI. **Four things to settle:** (a) **EXIF is non-negotiable** — phone photos carry GPS coordinates, and this app already holds colleagues' home pickup locations; strip metadata server-side before storing, or a group feed quietly publishes where people live; (b) storage quota and cost on the free tier once a group posts photos every day; (c) moderation — who deletes a post, and does the group admin get takedown over someone else's content? (d) retention — the feed grows forever unless something trims it. |
| D-34 | **Event groups (developer: not a priority, clarified 2026-08-28).** A second *class* of group: large, with **no fixed destination**, acting as a platform for coordinating car trips to a place. Someone in the group **creates an event**, and **cars subscribe to that event**. The developer's words: "you can have a group that someone can create an event and cars are suscribe to that event" | A new `event` entity between group and trip — **not** a per-trip origin/destination override on the existing model | **Open (raised 2026-08-28, clarified the same day)** — **Supersedes the first reading of this entry**, which had it as "each trip picks its own destination". That was wrong and would have produced the wrong schema: the unit people coordinate around is the **event** (a place plus a time that many cars converge on), not the trip. Shape: `event` rows owned by a group (title, destination, when), and `trip.event_id` so a car subscribes to one. A trip's destination then comes from its event rather than from the group. **What that leaves unanswered:** (a) `direction` (`out`/`back`/`round`) presumes a fixed home-and-destination pair — an event trip probably has an origin per car and no "back leg" in the same sense; (b) do event groups keep points and the leaderboard? The scoring model rewards a *commuting habit*, which one-off events are not; (c) can one group hold both fixed commutes and events, or is `kind` exclusive? (d) do D-29 stops apply, given a stop is defined as mid-leg on a fixed route? **This still amends `CLAUDE.md` §4** ("the route is owned by the group"), but only for the new class. **Explicitly not urgent** — the developer said so when clarifying. |
| D-35 | **How a two-way trip's return leg starts (developer: MOST URGENT, raised 2026-08-28)** — the lifecycle is `scheduled → started → closed \| cancelled` with a single `started_at` and a single `closed_at`, but a round trip has **two departures**: the outbound at `depart_at` and the return at `return_at` | **Lazily-materialised second leg** — the developer's design, recorded 2026-08-29 | **Answered by the developer (2026-08-29); four sub-questions still open.** **The design, in their words:** (1) people keep publishing **round trips by default**; (2) the driver taps **Start** and the riders are notified; (3) when the driver **finishes the outbound**, the system **automatically generates a back trip** carrying the riders who said they were returning with that driver; (4) the driver then **starts that back trip** like any other; (5) a rider who declines the return **frees a seat on the back trip**. **How the declaration is made:** joining a round trip **asks at join time** whether you are returning with the same driver; everyone else **learns about a freed return seat only once the outbound is over**, never before. **Assessment:** this is option (b) from the original entry — two rows, one per leg — but *materialised lazily at outbound close* rather than up front. That removes option (b)'s main cost (two cards in the feed for a ride that has not happened) while keeping its benefit: each row has exactly one departure, so **all five recorded breakages dissolve** — Start is unambiguous, points settle per leg, D-16's T−2h guard and the 6h auto-close and D-23's 24h expiry all measure against the correct `depart_at`. It also **very nearly resolves [D-30]**: return-only booking cannot happen before the outbound closes because the back trip does not exist yet, and outbound riders hold priority by construction rather than by a reservation rule. **What the design newly depends on, and the risk that follows:** the return leg's *existence* now hangs on the driver tapping **Close** on the outbound. If they forget, no back trip is ever created and every rider who declared a return is stranded with no card and no notice — and because freed seats surface only at that same moment, a Close at 17:45 releases seats for an 18:00 departure that nobody can plan around. **Recommendation:** the back trip is created at outbound close **or** at a deadline, whichever comes first, with `/api/cron/tick` owning the deadline — and the tick's existing 6h auto-close must spawn the back trip too, since the return is a rider commitment rather than a driver reward and should not be gated on the driver's diligence. **Second-order effect the developer should see before it ships — this is a scoring change, not only a lifecycle change.** Points go to the **driver** at close (`computeCloseAwards`: 1 `drive` + 1 escalating `pool` per confirmed rider). Two closes therefore pay a round trip **twice** where it used to pay once, and a rider on both legs is prompted for kudos **twice on the same driver on the same day** (each kudos itself scaled by car fullness, D-19). That may well be *more* correct — the driver really did drive twice — and it puts round trips on par with two one-ways, but historic ledger rows are never rewritten, so the leaderboard will hold old-rate and new-rate rides side by side. **All four questions answered by the developer (2026-08-29):** (a) the back trip pays the driver a **full** award — a round trip is now worth two closes, and the leaderboard will hold old-rate and new-rate rides side by side, accepted knowingly; (b) **kudos is per ride, not per leg** — one prompt per rider per ride, so the close of the *back* trip must not re-prompt riders already prompted on the outbound, and `computeKudosAward`'s fullness scaling needs a defined rider count for a two-leg ride; (c) a rider may change their return answer freely and **at no cost before the trip starts** (the remaining sliver: after the outbound *starts* but before the back trip exists — treat as the normal −5 late-cancellation window measured against `return_at`, unless the developer says otherwise); (d) the back trip **inherits capacity and the D-29 return stop**, and the **driver may move its departure time** — "the driver is in charge and can do whatever it wants". **Three further mechanics the developer added (2026-08-29):** (i) **close is no longer driver-only** — a group admin may close a ride the driver forgot to close. *(Originally recorded as "any rider, or a group admin"; **narrowed to the admin alone by the developer on 2026-08-30**: "you told me that it was dangerous the riders close trip and we decided to leave it up to driver and admin". Riders cannot close — see below.)*; (ii) if nobody closes it, the back trip is **generated automatically at T−2h before the return departure**; (iii) a driver **cannot publish a second trip at the same hour** as one they already have — see [D-36]. **Why riders were removed (2026-08-30), and what it costs:** `transition()` rejects every non-driver with `not_driver`, and close is also where the driver names who actually rode — any active rider left out of `confirmedTripRiderIds` is marked `no_show` and charged D-19's penalty. So a rider-initiated close, implemented as-is, would let one colleague mark another a no-show and award the driver points. The restricted close (answer (A)) removes the no-show hazard, but not the deeper one: a close moves points and decides who rode, and that is an authority a passenger should not hold over another passenger. **Built as: driver or group admin only.** The restricted close confirms **every** active rider, can mark nobody as a no-show, and writes `audit_log` with the actual actor (distinct from the system's null actor). The driver keeps the full close with its no-show judgement. **The cost is concentration of the safety net:** with riders excluded and mechanic (ii) deferred, the *only* thing standing between a forgotten close and a stranded return leg is an admin noticing. That holds until the T−2h auto-generation ships, and it is the strongest argument for shipping it soon. **(ii) BUILT 2026-08-30.** `/api/cron/tick` job 2 of 4: a `round` trip still `started` within `RETURN_LEG_LEAD_MINUTES` (120) of its `return_at`, with no leg yet, gets the restricted close — everyone confirmed, nobody a no-show, driver paid, leg materialised, `audit_log` action `cron_generate_return_leg` with a null actor. The scheduler acts as **nobody**: `TripTransitionActor.profileId` became optional and `isSystem` grants close alone, so an actor with no id and no flag is still refused. **The ordering problem found while building it, which the entry had not anticipated:** the existing 6h auto-close would have reached a normal commute *first* — out 08:00, back 18:00 puts the stale mark at 14:00 and the deadline at 16:00 — closed it for zero points and left the return unbuilt, which is this mechanic's own failure arriving through the other door. Fixed by running generation before the auto-close and having the auto-close **skip any round trip still owed a leg**. **What (ii) costs:** if the tick spawns the back trip while the outbound is still `started`, the outbound never closes on its own and its award is never paid — the driver drove twice and is paid once, then the 6h auto-close ends it for zero. The auto-generation must therefore **also close the outbound** in the restricted form above. Two edges to handle: an outbound still `scheduled` at T−2h before the return spawns **nothing** (the ride never happened; D-23's expiry owns it), and a `return_at` already inside T−2h when the outbound closes creates the back trip **immediately**. **Schema note:** the two rows want a `trip.parent_trip_id` (or a shared ride id) so the Past section, per-ride kudos (b) and any reporting can tell that two cards were one commute. **Four contradictions with what is already built or already decided (2026-08-29, developer confirmed "the admin can close any trip to generate what is missing"):** **(1) the admin close already exists and does the opposite of what this needs.** `POST /api/admin/trips/:id/force-close` is built and audit-logged, but it **deliberately never touches `points_ledger`** — its own comment: "no driver confirmation of who actually rode, same rule as the cron auto-close... an operational fix, not a substitute for the real close flow." Generating the missing back trip is compatible with that (spawning is not a points operation), but *paying the outbound* is not. So the route cannot simply be reused: either it gains the restricted-close behaviour recommended above (confirm every active rider, mark nobody `no_show`, real actor in `audit_log`), or the driver's forgotten outbound is force-closed for **zero points** and mechanic (ii)'s unpaid-award problem returns through the admin path instead of the cron path. **(2) there are now four spawn paths and no idempotency guard.** Driver close, rider close, admin force-close and the T−2h tick can each create the back trip. Nothing on the row records that the return was already materialised — `direction` stays `'round'` and `return_at` stays set — so any two of them racing produce **two** back trips. `parent_trip_id` is therefore load-bearing rather than cosmetic and wants a uniqueness constraint on it. **(3) `CLAUDE.md` §4 still contradicts answer (b).** The constant reads "Kudos: binary, **one per rider per trip**"; a ride is now two trips, so the file as written mandates two prompts where the decision says one per *ride*. That file is immutable to the agent — the developer edits it by hand, as with the D-19 scoring line that is already stale there. **(4) mechanic (ii) is blocked on [D-21].** T−2h auto-generation runs in `/api/cron/tick`, which still has no caller until the Supabase Vault secrets are set. Until then the *only* safety net for a forgotten outbound is the admin closing it by hand — which is (1) above, and which is why (1) is the piece to fix first. **Four implementation answers (developer, 2026-08-30):** **(A) The restricted close pays the full award.** One shared code path for the admin close and the T−2h tick (the rider close was dropped on 2026-08-30, see mechanic (i)): it confirms **every** active rider, can mark **nobody** as a `no_show`, pays the driver the normal `drive` + escalating `pool` award, and writes `audit_log` with the real actor. `force-close` gains this behaviour, so its "never touches `points_ledger`" rule is **deliberately retired for trips** — contradiction (1) above is resolved in favour of paying the driver. The driver keeps the full close with its no-show judgement; only the driver can call someone a no-show. **(B) Kudos fires at the back trip's close, scaled by the fuller leg.** One prompt per rider per ride. Riders who did **not** return are prompted at the outbound close instead, so nobody is skipped; riders who did are prompted only once, at the end. `computeKudosAward`'s multiplier is the **larger** of the two legs' confirmed-rider counts, so a driver is not penalised because two people walked home. **(C) The return question is asked outright at join time — no default, no opt-in/opt-out.** The developer: "when they want to register in a round trip, they will be asked if they want to get back in the same car... if they register, when they register they will be asked." So the join sheet puts the question as an explicit choice that must be answered before the join can be submitted; there is no pre-ticked box either way, because a default is the thing that complicates it. Schema follows: the flag is **not null with no default**, so it cannot be silently skipped. **APPLIED to the live database 2026-08-30** (`supabase db push`, migration `0013`; `migration list` shows 0013 on both sides and the remote reports up to date). **One thing changed at push time — a deploy shim.** `0013` was written to *drop* the two-argument `join_trip`, so a caller that forgets the answer fails loudly rather than silently defaulting to "not returning". That is right for application code and wrong for a live database: the deployed build is whatever is on `origin/main` when the migration runs, and it still calls the two-argument form — dropping it breaks **every join on the live app** from the instant the migration lands until a new build is serving. The old signature therefore survives as a two-line delegation passing `false`. It is not a default in disguise: the API layer requires the answer in zod before the function is ever reached, and `src/types/database.ts` declares only the three-argument version, so no TypeScript caller can reach the shim. **Shim removed 2026-08-30** by migration `0015`, once the developer confirmed the join sheet was asking the return question. `join_trip` is a single three-argument function again, and a caller that omits the answer fails loudly rather than defaulting to "not returning" — the original intent, restored as soon as the deploy window closed. **(D) First slice = close + spawn + the join question.** The T−2h auto-generation (mechanic (ii)) was deferred to a follow-up — **and then built the same day**, see (ii) above. What remains untested is not the code but the environment: the tick has no caller until the Vault secrets are set (D-21), so the generator has never run on a schedule. |
| D-36 | **A driver cannot publish two trips at the same hour (developer, 2026-08-29)** — "if a driver has an existing trip published, it can't publish the same trip to the same hour. That way we avoid duplicating trips" | A uniqueness rule on (driver, departure hour), enforced in the database rather than only in the form | **Open (raised 2026-08-29)** — the intent is clear and uncontested; what is undefined is the **key**. Candidates: driver + `date_trunc('hour', depart_at)` alone, or that plus `group_id`, or plus `direction`. It matters: a driver in two groups doing one 08:00 commute is arguably one trip and arguably two publishes, and an `out` at 08:00 alongside a `round` at 08:00 is a duplicate while an `out` at 08:00 alongside a `back` at 18:00 plainly is not. Cancelled and expired trips must be excluded from the constraint, or a driver who cancels an 08:00 ride can never republish one. **Interaction with [D-35], and it is a real one:** D-35 has the *system* create a back trip at `return_at`. If the driver has already published a one-way `back` at that hour by hand, a naive unique constraint makes the system's own insert fail — and the failure mode is a stranded return leg, exactly what D-35 exists to prevent. The generator must therefore look for an existing back trip at that hour and **attach the returning riders to it** rather than insert a second one, or the constraint must be scoped so the two cannot collide. **Question:** what is the key, and does a same-hour publish get a hard rejection or a "you already have a trip then — did you mean this one?" prompt that offers the existing trip? |

## Notes

- D-01, D-02, D-06, D-08, D-09, D-10 are marked "Applied" because they're already embodied in
  committed code (the dependency set; `group.cost_split_note`; `trip_rider.guest_name`;
  `group.late_window_minutes`/`late_penalty`), not open judgment calls — this is recording reality,
  not proceeding on an assumption.
- D-15 had no corresponding row in `00_DEV_ENVIRONMENT_SETUP.md` §9 even though
  `02_IMPLEMENTATION_PLAN.md` cited it as blocking Phase 1 alongside D-04 and D-13. Confirmed with
  the developer as a documentation error and dropped.
- Phase 0 (design tokens + component primitives) depended only on D-02, applied session 2. Phase 1
  (Supabase schema, RLS, auth) depended on D-04, D-13, D-15 — all now resolved, so Phase 1 work
  proceeds. Phases 2+ still stop at their listed open decisions until answered here.
- Build order deviates from `02_IMPLEMENTATION_PLAN.md` §2 as of 2026-08-16: Phase 6 (Maps) moves
  to the end, after Phase 9, per the developer. That file is immutable to the agent (proposed edits
  go here instead, per `CLAUDE.md`), so the phase table there still shows the original order — this
  note is the actual sequencing to follow.
- No Vercel deployment exists yet. Env vars that only make sense once one does (a live
  `NEXT_PUBLIC_APP_URL`, `CRON_SECRET` for a deployed cron endpoint, VAPID keys tied to a real
  push origin) are expected to stay placeholder/blank in `.env.local` for now — `src/env.ts`
  should only hard-require what local dev actually needs today (Supabase connection details),
  not deploy-time values. **Superseded 2026-08-16:** the developer populated `SUPABASE_ACCESS_TOKEN`,
  `SUPABASE_DB_PASSWORD`, and (already present) `VAPID_SUBJECT`/`NEXT_PUBLIC_VAPID_PUBLIC_KEY`/
  `VAPID_PRIVATE_KEY`/`CRON_SECRET`, and the Phase 1 schema is now genuinely live on the project (see
  below) — `src/env.ts` requires the VAPID/cron vars now that Phase 5 code actually reads them.
- **2026-08-16, discovered mid-session:** `0001_init.sql` (the entire Phase 1 schema) had never
  actually been applied to the live Supabase project — Docker was unavailable for local Postgres all
  session, and there was no CLI auth to push to the remote either, so every prior "Phase 1 done"
  claim was code-complete but not database-real. Fixed by linking the CLI with a developer-supplied
  access token + DB password and running `supabase db push` for real; `0002_join_trip.sql` and
  `0003_notification_reminder_type.sql` (Phase 4/5) went out the same way. `src/types/database.ts`
  is now generated from the real live schema (`pnpm db:types:linked`), not hand-written.
- **`VAPID_SUBJECT` in `.env.local` is not a valid `mailto:`/`https:` value** — `web-push` requires
  one of those two schemes and rejects anything else (confirmed live: a push send throws
  `Vapid subject is not an https: or mailto: URL`). Doesn't block anything that doesn't actually
  send a push (fixed a related bug this session — VAPID config is now lazy, not module-load-time —
  so this no longer crashes unrelated routes), but it does mean no push will actually deliver until
  the developer sets it to a real `mailto:you@yourdomain.com` or `https://yoursite/contact` value.
- **G6 (push verified on a real device) is not satisfiable in this agent's environment** — no
  physical device, and the headless browser pane's `Notification.permission` defaults to `"denied"`
  with no way to grant it (confirmed live). Manifest, service worker registration, subscribe/
  unsubscribe routes, and the cron reminder/auto-close logic are all verified live against the real
  database; actual push delivery to a device is not. This needs a real phone/browser test before G6
  can be claimed.
